// Angular Dependencies
import { Component } from '@angular/core';

/**
 * @author Soumit Mandal
 * Root level component
 */
@Component({
  selector: 'app-root',
  template: `
    <app-atm-locator></app-atm-locator>
           `
})
export class AppComponent {
}
